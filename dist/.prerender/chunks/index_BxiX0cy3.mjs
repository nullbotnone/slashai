import { c as createComponent } from './astro-component_CM5sHQaA.mjs';
import 'piccolore';
import { a as renderTemplate, r as renderComponent, m as maybeRenderHead, b as addAttribute } from './prerender_DGualgz_.mjs';
import { $ as $$BaseLayout } from './BaseLayout_rjQaa7Nu.mjs';
import { g as getCollection } from './_astro_content_DU-8pFiy.mjs';

var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(raw || cooked.slice()) }));
var _a;
const $$Index = createComponent(async ($$result, $$props, $$slots) => {
  const tutorials = (await getCollection("tutorials")).sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());
  return renderTemplate(_a || (_a = __template(["", "   <script>\n  const searchInput = document.getElementById('tutorial-search');\n  const grid = document.getElementById('tutorial-grid');\n  const noResults = document.getElementById('no-results');\n\n  searchInput.addEventListener('input', () => {\n    const search = searchInput.value.toLowerCase();\n    const cards = grid.querySelectorAll('.tutorial-card');\n    let visible = 0;\n\n    cards.forEach(card => {\n      const title = card.dataset.title;\n      const desc = card.dataset.desc;\n      const tool = card.dataset.tool;\n      \n      if (!search || title.includes(search) || desc.includes(search) || tool.includes(search)) {\n        card.style.display = '';\n        visible++;\n      } else {\n        card.style.display = 'none';\n      }\n    });\n\n    noResults.style.display = visible === 0 ? 'block' : 'none';\n  });\n\n  // Scroll animation observer\n  const scrollObserver = new IntersectionObserver((entries) => {\n    const visible = entries.filter(e => e.isIntersecting).sort((a, b) => {\n      return a.boundingClientRect.top - b.boundingClientRect.top;\n    });\n    visible.forEach((entry, i) => {\n      entry.target.style.animationDelay = `${i * 100}ms`;\n      entry.target.classList.add('animate-in');\n      scrollObserver.unobserve(entry.target);\n    });\n  }, { threshold: 0.1, rootMargin: '0px 0px -50px 0px' });\n\n  document.querySelectorAll('.card').forEach(card => {\n    scrollObserver.observe(card);\n  });\n<\/script>"], ["", "   <script>\n  const searchInput = document.getElementById('tutorial-search');\n  const grid = document.getElementById('tutorial-grid');\n  const noResults = document.getElementById('no-results');\n\n  searchInput.addEventListener('input', () => {\n    const search = searchInput.value.toLowerCase();\n    const cards = grid.querySelectorAll('.tutorial-card');\n    let visible = 0;\n\n    cards.forEach(card => {\n      const title = card.dataset.title;\n      const desc = card.dataset.desc;\n      const tool = card.dataset.tool;\n      \n      if (!search || title.includes(search) || desc.includes(search) || tool.includes(search)) {\n        card.style.display = '';\n        visible++;\n      } else {\n        card.style.display = 'none';\n      }\n    });\n\n    noResults.style.display = visible === 0 ? 'block' : 'none';\n  });\n\n  // Scroll animation observer\n  const scrollObserver = new IntersectionObserver((entries) => {\n    const visible = entries.filter(e => e.isIntersecting).sort((a, b) => {\n      return a.boundingClientRect.top - b.boundingClientRect.top;\n    });\n    visible.forEach((entry, i) => {\n      entry.target.style.animationDelay = \\`\\${i * 100}ms\\`;\n      entry.target.classList.add('animate-in');\n      scrollObserver.unobserve(entry.target);\n    });\n  }, { threshold: 0.1, rootMargin: '0px 0px -50px 0px' });\n\n  document.querySelectorAll('.card').forEach(card => {\n    scrollObserver.observe(card);\n  });\n<\/script>"])), renderComponent($$result, "BaseLayout", $$BaseLayout, { "title": "Tutorials", "description": "Step-by-step tutorials for AI tools, agents, and automation workflows.", "data-astro-cid-hwtq5dlv": true }, { "default": async ($$result2) => renderTemplate` ${maybeRenderHead()}<section class="hero" data-astro-cid-hwtq5dlv> <div class="container" data-astro-cid-hwtq5dlv> <h1 data-astro-cid-hwtq5dlv><span class="gradient-text" data-astro-cid-hwtq5dlv>Tutorials</span></h1> <p data-astro-cid-hwtq5dlv>Step-by-step guides to set up and use AI tools. Practical, tested, beginner-friendly.</p> </div> </section> <section class="section-glow" data-astro-cid-hwtq5dlv> <div class="container" data-astro-cid-hwtq5dlv> <input type="text" id="tutorial-search" placeholder="🔍 Search tutorials..." class="blog-search" data-astro-cid-hwtq5dlv> <div class="card-grid" id="tutorial-grid" data-astro-cid-hwtq5dlv> ${tutorials.map((post) => renderTemplate`<a${addAttribute(`/tutorials/${post.id.replace(/\.md$/, "")}/`, "href")} class="card tutorial-card"${addAttribute(post.data.title.toLowerCase(), "data-title")}${addAttribute(post.data.description.toLowerCase(), "data-desc")}${addAttribute(post.data.toolName.toLowerCase(), "data-tool")} style="text-decoration: none; color: inherit;" data-astro-cid-hwtq5dlv> <h3 data-astro-cid-hwtq5dlv>${post.data.title}</h3> <p data-astro-cid-hwtq5dlv>${post.data.description}</p> <div style="display: flex; gap: 0.5rem; flex-wrap: wrap; margin-top: 0.5rem;" data-astro-cid-hwtq5dlv> <span class="tag" data-astro-cid-hwtq5dlv>${post.data.toolName}</span> <span class="tag" data-astro-cid-hwtq5dlv>${post.data.difficulty}</span> ${post.data.timeToComplete && renderTemplate`<span class="tag" data-astro-cid-hwtq5dlv>⏱️ ${post.data.timeToComplete}</span>`} </div> </a>`)} </div> <p class="no-results" id="no-results" style="display: none; text-align: center; color: var(--color-text-secondary); padding: 3rem 0;" data-astro-cid-hwtq5dlv>
No tutorials found matching your search.
</p> </div> </section> ` }));
}, "/home/rpi/.openclaw/workspace/projects/slashai/slashai/src/pages/tutorials/index.astro", void 0);

const $$file = "/home/rpi/.openclaw/workspace/projects/slashai/slashai/src/pages/tutorials/index.astro";
const $$url = "/tutorials";

const _page = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
  __proto__: null,
  default: $$Index,
  file: $$file,
  url: $$url
}, Symbol.toStringTag, { value: 'Module' }));

const page = () => _page;

export { page };
